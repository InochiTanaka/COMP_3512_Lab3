#include "Matrix.hpp"

int main()
{
	Matrix a;
	cout << a << endl;

	Matrix b;
	cout << b << endl;

	//int[] wrong_size= { 1,2,3 };
	//Matrix c(wrong_size);

	//b.set_value{ 0, 0, -1 };
	//b get_value{ 0, 0 };
	//b.clear();

	//Matrix identity = a.identity(5);
	return 0;
};